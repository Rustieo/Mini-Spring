<?xml version="1.0" encoding="UTF-8"?>
<mapper namespace="com.minis.test.mapper.UserMapper">
    <select id="getUserInfo" parameterType="java.lang.Integer" resultType="com.minis.test.entity.User">
        select id, name, birthday
        from users
        where id=?
    </select>
</mapper>